"use client";
import React from 'react';

// Token expiry warning disabled in simplified auth system
// Users will be logged out when token expires (cleaner UX)
const TokenExpiryWarning: React.FC = () => {
  return null;
};

export default TokenExpiryWarning;